var my_name = "Tuna";   // only use var if we must support older browser, also var is global scope
let aNumber = 12;       
const pi_num = 3.1413;  // must be assigned when declare const

console.log(my_name);


const cars = ["Toyota", "Vinfast", "Volvo"];
const my_car_info = {name:"Audi", color:"black"};

console.log(cars[0]);
console.log(my_car_info.color)